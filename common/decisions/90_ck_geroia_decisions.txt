form_thonian_kingdom_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_found_kingdom.dds"
	}
	decision_group_type = major
	ai_check_interval = 18
	desc = form_thonian_kingdom_decision_desc
	
	cost = {
		gold = 200
	}
	
	is_shown = {
		OR = {
			has_culture = culture:thonian
			culture = { any_parent_culture_or_above = { this = culture:thonian } }
		}
		NOT = {
			exists = title:k_thlos.holder
		}
		is_landed = yes
	}
	
	is_valid = {
		is_independent_ruler = yes
		custom_tooltip = {
			text = thlos_holds_at_least_two_duchies_tt
			any_held_title = {
				count >= 2
				tier = tier_duchy
			}
		}
		custom_tooltip = {
			text = owns_half_thonian_lands_tt
			any_county_in_region = {
				region = region_thlos
				percent >= 0.5
				holder.top_liege = {
					this = root
				}
			}
		}
	}
	
	is_valid_showing_failures_only = {
		is_independent_ruler = yes
		is_landless_adventurer = no
		is_capable_adult = yes
		is_imprisoned = no
		is_at_war = no
	}

	effect = {
		
		custom_tooltip = you_will_unify_thonians.tt
		
		
		create_title_and_vassal_change = {
			type = created
			save_scope_as = title_change
			add_claim_on_loss = no
		}
		title:k_thlos = {
			change_title_holder = {
				holder = root
				change = scope:title_change
			}
		}
		resolve_title_and_vassal_change = scope:title_change
		
		custom_tooltip = {
			text = thlos_de_jure_changes_tt
			title:d_kefalida = {
				set_de_jure_liege_title = title:k_thlos
			}
			title:d_modeia = {
				set_de_jure_liege_title = title:k_thlos
			}
			title:d_imiaugia = {
				set_de_jure_liege_title = title:k_thlos
			}
			title:d_koszanos = {
				set_de_jure_liege_title = title:k_thlos
			}
		}
		
		trigger_event = ck_geroian_events.0012
	}

	ai_potential = {
		is_independent_ruler = yes
		is_landless_adventurer = no
		highest_held_title_tier >= tier_duchy
	}

	ai_will_do = {
		base = 100
	}
}