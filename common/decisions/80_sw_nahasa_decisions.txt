oroki_bathe_in_waters = {
	ai_check_interval = 0
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_wash_self.dds"
	}
	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { faith.religion = religion:oroki_religion }
				desc = oroki_bathe_in_waters_desc
			}
			triggered_desc = {
				trigger = {
					NOT = {
						faith.religion = religion:oroki_religion
					}
				}
				desc = oroki_bathe_in_waters_desc
			}
		}
		triggered_desc = {
			trigger = {
				is_pregnant = yes
			}
			desc = oroki_bathe_in_waters_desc_pregnant
		}
	}
	decision_group_type = decisions

	is_shown = {
		OR = {
			location = {
				has_building = olokun
			}
			any_sub_realm_barony = {
				title_province = {
					has_building = olokun
				}
			}
		}
	}
	is_valid = {
	}
	is_valid_showing_failures_only = { 
		is_travelling = no
		is_physically_able = yes
	}
	
	cooldown = { years = 5 }

	effect = {
		send_interface_toast = {
			type = event_toast_effect_good
			title = oroki_bathe_in_waters_notification
			left_icon = root

			add_character_modifier = {
				modifier = bathed_in_olokun_river
				years = 3
			}
			add_stress = -75
		}
	}

	ai_potential = {
		always = no
	}

	ai_will_do = {
		base = 0
	}
}
read_the_mahdi_tomes = {
	ai_check_interval = 0
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_misc.dds"
	}
	desc = read_the_mahdi_tomes_desc
	decision_group_type = decisions

	is_shown = {
		is_ai = no
		faith = {
			has_doctrine = tenet_path_of_the_mahdi
		}
	}
	
	is_valid_showing_failures_only = { 
		is_travelling = no
		is_physically_able = yes
	}
	
	cooldown = { months = 4 }

	effect = {
		custom_tooltip = read_the_mahdi_tomes_effect_tt
		start_travel_plan = {
			destination = province:5916
			on_travel_planner_cancel_on_action = read_the_mahdi_tomes_planner_cancel_on_action
			on_arrival_event = west_nahasa.0001
			on_arrival_destinations = all_but_last
		}
		hidden_effect = {
			if = {
				limit = {
					exists = var:ck_mahdi_read_tomes
					has_variable = ck_mahdi_read_tomes
				}
				remove_variable = ck_mahdi_read_tomes # Ensure the player doesn't get stressed off the get go
			}
		}
	}

	ai_potential = {
		always = no
	}

	ai_will_do = {
		base = 0
	}
}
declare_myself_the_mahdi = {
	ai_check_interval = 0
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_legitimacy.dds"
	}
	desc = declare_myself_the_mahdi_desc
	decision_group_type = major

	is_shown = {
		is_ai = no
		faith = {
			has_doctrine = tenet_path_of_the_mahdi
		}
		has_variable = mahdi_step
		var:mahdi_step >= 3 # Show the player once they near the end of it
	}
	is_valid = {
		top_liege = this
		is_landed = yes
		custom_tooltip = {
			text = control_over_half_of_the_west_nahasa_tt
			any_county_in_region = {
				region = wb_province_west_nahasa
				percent > 0.3
				holder.top_liege = root
			}
		}
		custom_tooltip = {
			text = control_over_half_of_the_west_nahasa_no_one_else
			any_county_in_region = {
				region = wb_province_west_nahasa
				percent > 0.3
				holder.top_liege = {
					NOT = {
						this = root
					}
				}
			}
		}
	}
	is_valid_showing_failures_only = { 
		is_travelling = no
		is_physically_able = yes
		custom_tooltip = {
			text = finished_all_mahdi_steps_tt
			has_variable = mahdi_step
			var:mahdi_step >= 5 # Show the player once they near the end of it
		}
	}
	
	cooldown = { months = 2 }

	effect = {
		trigger_event = {
			id = west_nahasa.0003
			days = 2
		}
	}

	ai_potential = {
		always = no
	}

	ai_will_do = {
		base = 0
	}
}